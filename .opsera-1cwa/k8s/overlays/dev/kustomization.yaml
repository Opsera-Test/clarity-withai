# ════════════════════════════════════════════════════════════════════════════
# KUSTOMIZATION - DEV Environment Overlay
# Generated by Code-to-Cloud Enterprise v0.901
# ════════════════════════════════════════════════════════════════════════════
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: opsera-1cwa-dev

resources:
  - ../../base
  - namespace.yaml

labels:
  - pairs:
      environment: dev
      tenant: opsera

images:
  - name: 1cwa
    newName: 792373136340.dkr.ecr.us-west-2.amazonaws.com/opsera/1cwa
    newTag: 454c827-20260131164138

patches:
  # Patch Ingress hostname for DEV
  - target:
      kind: Ingress
      name: 1cwa
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: opsera-1cwa-dev.agent.opsera.dev
      - op: replace
        path: /spec/tls/0/hosts/0
        value: opsera-1cwa-dev.agent.opsera.dev

  # Patch Deployment replicas for DEV
  - target:
      kind: Deployment
      name: 1cwa
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
