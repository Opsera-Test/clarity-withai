# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - DEV Environment
# Generated by Code-to-Cloud Enterprise v0.901
# RULE 59: Use destination.name for hub-spoke architecture
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: 1cwa-dev
  namespace: argocd
  labels:
    tenant: opsera
    app: 1cwa
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    # RULE 58: Resolved placeholder - actual repo URL
    repoURL: https://github.com/opsera-agentic/clarity-withai
    targetRevision: HEAD
    path: .opsera-1cwa/k8s/overlays/dev

  destination:
    # RULE 59: CRITICAL - Use 'name' for spoke cluster, NOT 'server'
    # This ensures deployment goes to spoke cluster, not hub
    name: opsera-usw2-np
    namespace: opsera-1cwa-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
