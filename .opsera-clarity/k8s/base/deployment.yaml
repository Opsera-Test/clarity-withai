# ════════════════════════════════════════════════════════════════════════════
# DEPLOYMENT - Base Kubernetes Deployment
# Generated by: Opsera Code-to-Cloud Enterprise v0.901
# RULE 112: No imagePullSecrets needed for EKS with IAM/IRSA permissions
# ════════════════════════════════════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clarity
  labels:
    app: clarity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: clarity
  template:
    metadata:
      labels:
        app: clarity
    spec:
      # RULE 112: No imagePullSecrets needed - EKS uses IAM/IRSA for ECR access
      containers:
        - name: clarity
          image: clarity:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          # RULE 79: SPA health check is /
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
