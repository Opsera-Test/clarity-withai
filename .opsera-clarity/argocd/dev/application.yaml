# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - DEV Environment
# Generated by: Opsera Code-to-Cloud Enterprise v0.901
# RULE 59: Use destination.name for hub-spoke (NOT destination.server)
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: clarity-dev
  namespace: argocd
  labels:
    tenant: opsera
    app: clarity
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: PLACEHOLDER_REPO_URL
    targetRevision: HEAD
    path: .opsera-clarity/k8s/overlays/dev

  destination:
    # RULE 59: Use 'name' for spoke cluster, NOT 'server'
    # This ensures deployment goes to the spoke cluster, not the hub
    name: opsera-usw2-np
    namespace: opsera-clarity-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
