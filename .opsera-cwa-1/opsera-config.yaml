# ════════════════════════════════════════════════════════════════════════════
# OPSERA CONFIG - Single Source of Truth (RULE 60)
# Generated by Code-to-Cloud Enterprise v1.7.2
# ════════════════════════════════════════════════════════════════════════════

app:
  name: cwa-1
  tenant: opsera
  domain: agent.opsera.dev

cloud:
  provider: aws
  region: us-east-1
  region_short: use1

clusters:
  hub:
    name: argocd-use1
    type: hub
    role: ArgoCD control plane
  spoke:
    name: opsera-use1-np
    type: spoke
    role: Workload cluster

environments:
  dev:
    enabled: true
    namespace: opsera-cwa-1-dev
    hostname: opsera-cwa-1-dev.agent.opsera.dev
    tls_secret: opsera-cwa-1-dev-tls
    deployment_strategy: rolling

container_registry:
  type: ecr
  repository: opsera/cwa-1
  # Full URI constructed as: {account_id}.dkr.ecr.{region}.amazonaws.com/{repository}

ingress:
  class: nginx
  cluster_issuer: letsencrypt-prod
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"

settings:
  auto_promote: false  # RULE 65: Disabled until DEV verified
  force_sync: true     # RULE 66: Force ArgoCD sync
  diagnostics: true    # RULE 67: Enable diagnostics

# Generated: 2026-01-30
# Skill Version: Code-to-Cloud Enterprise v1.7.2
