# ════════════════════════════════════════════════════════════════════════════
# BASE INGRESS - clarity-withai-1
# Generated by Code-to-Cloud Enterprise v1.7
# RULE 107: ACM Wildcard certificates don't need TLS section in Ingress
# ════════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clarity-withai-1
  labels:
    app: clarity-withai-1
    tenant: opsera
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    # RULE 107: No ssl-redirect when ACM handles TLS at ALB
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  # RULE 107: No TLS section - ACM wildcard certificate terminates SSL at ALB
  rules:
    - host: PLACEHOLDER_SUBDOMAIN.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clarity-withai-1
                port:
                  number: 80
